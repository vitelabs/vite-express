import { connect } from '../utils/globalContext';
import { State } from '../utils/types';

type Props = State & {};

const _____ = ({}: Props) => {
	return (
		<div className="">
			<p></p>
		</div>
	);
};

export default connect(_____);
